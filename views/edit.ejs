<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post - SASTRA Connect</title>
    <link href="https://fonts.googleapis.com/css2?family=Overpass:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Overpass", sans-serif;
            background: linear-gradient(to right, #f5f7fa, #e0e7ff);
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }

        h1 {
            font-weight: 900;
            text-align: center;
            margin-bottom: 30px;
            font-size: 2.5rem;
            color: cornflowerblue;
            text-shadow: 1px 1px 2px #bbb;
        }

        form {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1), 0 15px 20px rgba(0, 0, 0, 0.05);
            display: flex;
            flex-direction: column;
        }

        label {
            font-weight: 600;
            margin-top: 15px;
            margin-bottom: 5px;
        }

        input[type="text"],
        textarea {
            padding: 10px 12px;
            border-radius: 10px;
            border: 1px solid #ccc;
            font-size: 1rem;
            font-family: "Overpass", sans-serif;
            resize: vertical;
        }

        textarea {
            min-height: 100px;
        }

        .btn-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .btn {
            padding: 12px 25px;
            font-size: 1rem;
            font-weight: 600;
            color: #fff;
            background-color: cornflowerblue;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
            text-align: center;
            text-decoration: none;
        }

        .btn:hover {
            background-color: #3a78d1;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.25);
        }

        .btn:active {
            transform: translateY(-1px);
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
        }

        .back-btn {
            background-color: #ff9800;
        }

        .back-btn:hover {
            background-color: #e68900;
        }

        @media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }

            form {
                padding: 20px;
            }

            .btn-container {
                flex-direction: column;
            }
        }

        span {
            color: black;
        }
    </style>
</head>

<body>
    <h1>Edit <span>Post</span></h1>
    <form action="/posts/<%= post.id %>?_method=PUT" method="POST">
        <label for="title">Title</label>
        <input type="text" name="title" id="title" value="<%= post.title %>" required>

        <label for="author">Name</label>
        <input type="text" name="author" id="author" value="<%= post.author %>" required>

        <label for="subject">Subject</label>
        <input type="text" name="subject" id="subject" value="<%= post.subject %>" required>

        <label for="content">Content</label>
        <textarea name="content" id="content" required><%= post.content %></textarea>

        <div class="btn-container">
            <button type="submit" class="btn">Update Post</button>
            <a href="/posts" class="btn back-btn">Back</a>
        </div>
    </form>
</body>

</html>
