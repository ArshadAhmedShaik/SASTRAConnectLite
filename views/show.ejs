<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detailed Post</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Overpass:ital,wght@0,100..900;1,100..900&display=swap"
        rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Overpass", sans-serif;
            background: linear-gradient(to right, #f5f7fa, #e0e7ff);
            color: #333;
            line-height: 1.6;
            padding: 30px;
        }

        h1 {
            font-weight: 900;
            font-size: 2.5rem;
            margin-bottom: 30px;
            text-align: center;
            color: #1e293b;
        }

        h1 span {
            color: cornflowerblue;
        }

        .container {
            background-color: #fff;
            border-radius: 15px;
            padding: 30px;
            max-width: 700px;
            margin: 0 auto;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.25s ease, box-shadow 0.25s ease;
            will-change: transform, box-shadow;
        }

        .container:hover {
            transform: translateY(-8px);
            box-shadow: 0 16px 25px rgba(0, 0, 0, 0.15);
        }

        .post-detail {
            margin-bottom: 15px;
        }

        .post-detail span {
            font-weight: 600;
            color: #1e293b;
        }

        .comments {
            margin-top: 20px;
        }

        .comments h3 {
            margin-bottom: 10px;
            font-size: 1.2rem;
            color: #1e293b;
        }

        .comment {
            background-color: #f1f5f9;
            padding: 10px 15px;
            border-radius: 10px;
            margin-bottom: 10px;
            box-shadow: 0 3px 8px rgba(0, 0, 0, 0.05);
        }

        /* Add Comment Form */
        .comment-form {
            margin-top: 25px;
            text-align: center;
        }

        .comment-form textarea {
            width: 100%;
            padding: 10px;
            border-radius: 10px;
            border: 1px solid #cbd5e1;
            resize: none;
            font-size: 1rem;
            font-family: inherit;
            outline: none;
            transition: border-color 0.2s ease, box-shadow 0.2s ease;
        }

        .comment-form textarea:focus {
            border-color: cornflowerblue;
            box-shadow: 0 0 5px rgba(100, 149, 237, 0.4);
        }

        .comment-form button {
            margin-top: 10px;
            padding: 10px 20px;
            font-weight: 600;
            background-color: cornflowerblue;
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: background-color 0.2s ease, transform 0.2s ease;
        }

        .comment-form button:hover {
            background-color: #3a78d1;
            transform: translateY(-2px);
        }

        .btn-container {
            text-align: center;
            margin-top: 25px;
        }

        .back-btn,
        .delete-btn {
            display: inline-block;
            margin: 10px;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: 600;
            color: #fff;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s ease;
            will-change: transform, box-shadow, background-color;
        }

        .back-btn {
            background-color: cornflowerblue;
        }

        .back-btn:hover {
            background-color: #3a78d1;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .delete-btn {
            background-color: #ef4444;
        }

        .delete-btn:hover {
            background-color: #dc2626;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(239, 68, 68, 0.3);
        }

        @media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }

            .container {
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    <h1><span>Post</span> Details</h1>

    <div class="container">
        <p class="post-detail"><span>Author:</span> <%= post.author %></p>
        <p class="post-detail"><span>Title:</span> <%= post.title %></p>
        <p class="post-detail"><span>Subject:</span> <%= post.subject %></p>
        <p class="post-detail"><span>Content:</span> <%= post.content %></p>

        <div class="comments">
            <h3>Comments:</h3>
            <% if (post.comments.length === 0) { %>
                <p>No comments yet.</p>
            <% } else { %>
                <% for (let comment of post.comments) { %>
                    <div class="comment"><%= comment.content %></div>
                <% } %>
            <% } %>
        </div>

        <div class="comment-form">
            <form action="/posts/<%= post.id %>/comments" method="POST">
                <textarea name="content" rows="3" placeholder="Write a comment..." required></textarea>
                <button type="submit">💬 Add Comment</button>
            </form>
        </div>

        <div class="btn-container">
            <form action="/posts/<%= post.id %>?_method=DELETE" method="POST"
                onsubmit="return confirm('Are you sure you want to delete this post?');"
                style="display:inline;">
                <button type="submit" class="delete-btn">🗑️ Delete Post</button>
            </form>

            <a href="/posts" class="back-btn">← Back</a>
        </div>
    </div>
</body>

</html>
